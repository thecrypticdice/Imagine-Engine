#include "headers/headers.h"

class player
{
    public:
    void init()
    {
        // initialize the player
    }
    void update(float delta_time)
    {
        // update the player
    }
    void draw()
    {
        // draw the player
    }
    private:
    Vector2 m_position;
    3D_camera m_camera;
};